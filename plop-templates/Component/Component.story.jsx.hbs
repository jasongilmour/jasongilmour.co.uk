import React from 'react';
import { storiesOf } from '@storybook/react';
import {{pascalCase name}} from './{{pascalCase name}}';

storiesOf('Components|{{pascalCase name}}', module).add('with text', () => (
    <{{pascalCase name}} title="{{pascalCase name}}" />
));